<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.intent.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!-- Permissions for external storage and media access -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />

    <!-- New permissions for Android 13 and above -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        tools:ignore="SelectedPhotoAccess" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO"
        tools:ignore="SelectedPhotoAccess" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- MainActivity Declaration -->
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:name=".GmScanApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />
        <meta-data
            android:name="com.facebook.sdk.AutoInitEnabled"
            android:value="true" />

        <meta-data
            android:name="com.google.android.gms.auth.api.credentials.ClientId"
            android:value="@string/default_web_client_id"/>

        <!-- Splash Activity -->
        <activity
            android:name=".activity.SplashActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity with LinkedIn callback handling -->
        <activity
            android:name=".activity.LoginActivity"
            android:launchMode="singleTask"
            android:exported="true">
            <!-- Intent filter for LinkedIn callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="gmscan.onrender.com"
                    android:pathPrefix="/auth/linkedin/callback" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.ForgotPasswordActivity" />
        <activity
            android:name=".activity.VerificationCodeActivity"
            android:exported="false" />
        <activity android:name=".activity.CreatePasswordActivity" />


        <activity
            android:name=".activity.WelcomeActivity"
            android:theme="@style/Theme.Splash" />

        <activity
            android:name=".activity.OnBoardActivity"
            android:theme="@style/Theme.Splash" />

        <activity android:name=".activity.SelectDocumentsActivity"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".activity.IDScanningActivity"
            android:label="@string/id_scan_activity" />
        <activity
            android:name=".activity.DocumentScanningActivity"
            android:label="@string/document_scan_activity" />
        <activity
            android:name=".activity.BusinessScanActivity"
            android:label="@string/business_scan_activity" />
        <activity
            android:name=".activity.BookScannerActivity"
            android:label="@string/book_scan_label" />
        <activity
            android:name=".activity.BusinessDetailsActivity"
            android:label="@string/business_detail_label" />
        <activity
            android:name=".activity.IDDetailsActivity"
            android:label="@string/details" />
        <activity
            android:name=".activity.ScanHistoryActivity"
            android:label="@string/scan_history" />
        <activity
            android:name=".activity.DocumentDetailsActivity"
            android:label="@string/document_details" />
        <activity
            android:name=".activity.BookDetailsActivity"
            android:label="@string/books_details_card" />
        <activity
            android:name=".activity.WebViewActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.gmscan.auth" android:host="callback" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LanguageActivity" />
        <activity android:name=".activity.SettingsActivity" />
        <activity android:name=".activity.AboutActivity" />
        <activity android:name=".activity.PersonalInfoActivity" />

        <meta-data
            android:name="com.google.ar.core"
            android:value="required" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.gmscan.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="document_ui" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>